<Window x:Class="Casium.Dialogs.PromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="System prompt"
        Width="600" Height="620"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource Brush.Bg.Base}"
        Foreground="{DynamicResource Brush.Text.Primary}"
        FontFamily="{StaticResource Font.Primary}"
        UseLayoutRounding="True"
        WindowStyle="None">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="48"
                            ResizeBorderThickness="0"
                            CornerRadius="0"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Border BorderBrush="{DynamicResource Brush.Window.Border}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    BorderBrush="{DynamicResource Brush.Border.Subtle}"
                    BorderThickness="0,0,0,1">
                <Grid>
                    <TextBlock Text="System prompt"
                               Style="{StaticResource Txt.H2}"
                               Margin="20,0,0,0"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Left"/>
                    <Button Content="&#xE8BB;"
                            Style="{StaticResource Btn.WindowClose}"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True"
                            HorizontalAlignment="Right"
                            Click="Close_Click"/>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="24,18,24,22">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Name" Style="{StaticResource Txt.Caption}"/>
                <TextBox x:Name="NameBox" Grid.Row="1" Margin="0,6,0,14"
                         Tag="e.g. Code reviewer"/>

                <TextBlock Grid.Row="2" Text="Instructions sent before every message" Style="{StaticResource Txt.Caption}"/>
                <TextBox x:Name="ContentBox" Grid.Row="3"
                         Margin="0,6,0,16"
                         AcceptsReturn="True" TextWrapping="Wrap"
                         VerticalContentAlignment="Top"
                         VerticalScrollBarVisibility="Auto"
                         FontFamily="{StaticResource Font.Mono}"
                         FontSize="12.5"
                         Tag="Describe how the AI should behave…"/>

                <Grid Grid.Row="4">
                    <Button Style="{StaticResource Btn.Ghost}" HorizontalAlignment="Left"
                            Click="Cancel_Click">
                        <TextBlock Text="Cancel" VerticalAlignment="Center"/>
                    </Button>
                    <Button Style="{StaticResource Btn.Primary}" HorizontalAlignment="Right"
                            Click="Save_Click" MinWidth="120">
                        <TextBlock Text="Save" VerticalAlignment="Center"/>
                    </Button>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
