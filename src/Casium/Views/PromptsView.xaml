<UserControl x:Class="Casium.Views.PromptsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Casium.Controls">

    <UserControl.Resources>
        <DataTemplate x:Key="PromptCard">
            <Border Style="{StaticResource Card}" Margin="0,0,0,10" Padding="18,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" Style="{StaticResource Txt.H3}"/>
                            <controls:Pill x:Name="DefaultPill" Text="default" Kind="Accent"
                                           Margin="12,0,0,0" VerticalAlignment="Center"
                                           Visibility="Collapsed"/>
                        </StackPanel>
                        <Border Background="{DynamicResource Brush.Bg.Sunken}"
                                BorderBrush="{DynamicResource Brush.Code.Border}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="12,10"
                                Margin="0,10,0,0">
                            <TextBlock Text="{Binding Content}"
                                       FontFamily="{StaticResource Font.Mono}"
                                       FontSize="11.5"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                       TextWrapping="Wrap"
                                       MaxHeight="86"
                                       TextTrimming="CharacterEllipsis"/>
                        </Border>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center"
                                Margin="18,0,0,0">
                        <Button Style="{StaticResource Btn.Secondary}" Click="UseInChat_Click"
                                ToolTip="Make this the prompt for new chats">
                            <TextBlock Text="Use in chat" VerticalAlignment="Center"/>
                        </Button>
                        <Button Style="{StaticResource Btn.Ghost}" Margin="0,6,0,0"
                                Click="EditPrompt_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE70F;"
                                           FontFamily="{StaticResource Font.Icons}"
                                           FontSize="12" VerticalAlignment="Center"/>
                                <TextBlock Text="Edit" VerticalAlignment="Center" Margin="7,0,0,0"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource Btn.Danger}" Margin="0,6,0,0"
                                Click="DeletePrompt_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE74D;"
                                           FontFamily="{StaticResource Font.Icons}"
                                           FontSize="12" VerticalAlignment="Center"/>
                                <TextBlock Text="Delete" VerticalAlignment="Center" Margin="7,0,0,0"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding IsDefault}" Value="True">
                    <Setter TargetName="DefaultPill" Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="24,18,24,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Prompts" Style="{StaticResource Txt.H1}"/>
                <TextBlock Text="System prompts shape how every model behaves — instructions that are sent before your messages"
                           Style="{StaticResource Txt.BodySecondary}" Margin="0,5,0,0"/>
            </StackPanel>
            <Button Grid.Column="1" Style="{StaticResource Btn.Primary}" Click="NewPrompt_Click"
                    VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE710;"
                               FontFamily="{StaticResource Font.Icons}"
                               FontSize="13" VerticalAlignment="Center"/>
                    <TextBlock Text="New prompt" VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>
            </Button>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,0,10,0">
                <ItemsControl x:Name="PromptList" ItemTemplate="{StaticResource PromptCard}"/>
                <Border x:Name="EmptyState" Style="{StaticResource Card.Sunken}" Padding="22"
                        Visibility="Collapsed">
                    <TextBlock Text="No prompts yet — create one to give your models a personality."
                               Style="{StaticResource Txt.BodySecondary}"/>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
